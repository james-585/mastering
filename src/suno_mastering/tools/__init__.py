"""Auxiliary tooling shipped alongside the mastering pipeline: orchestration
across stems, Demucs tuning/runtime helpers, and release-candidate audits.
Not imported by the core `suno_mastering` pipeline; used by the tests and
by the standalone entry points under this subpackage.
"""
